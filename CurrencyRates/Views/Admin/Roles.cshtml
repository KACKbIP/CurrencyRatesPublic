@model List<CurrencyRates.Models.Admin.Roles>
@{
    Layout = null;
}

<select id="roleId" class="form-control">

    @if (Model != null)
    {
        @foreach (var item in Model.OrderBy(m => m.Id))
        {
            <option value="@item.Id">@item.Name</option>
        }
    }
</select>
